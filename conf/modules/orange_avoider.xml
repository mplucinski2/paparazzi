<!DOCTYPE module SYSTEM "module.dtd">

<module name="orange_avoider">
  <doc>
    <description>
This module is an example module for the course AE4317 Autonomous Flight of Micro Air Vehicles at the TU Delft.
This module is used in combination with the optical flow module (cv_opticflow) and the navigation mode of the autopilot.
The avoidance strategy is to monitor the divergence from optical flow calculations. When the divergence exceeds a threshold
(given by oa_divergence_threshold), we assume that there is an obstacle in front of the drone and we turn.

The module binds to the OPTICAL_FLOW ABI message to receive divergence information.
    </description>
    <define name="ORANGE_AVOIDER_OPTICAL_FLOW_ID" value="ABI_BROADCAST" description="which OPTICAL_FLOW message to receive for divergence detection."/>
  </doc>
    <settings>
    <dl_settings>
      <dl_settings name="OrangeAvoider">
         <dl_setting var="oa_divergence_threshold" min="-0.5" step="0.005" max="0.5" shortname="div_threshold"/>
      </dl_settings>
    </dl_settings>
  </settings>
  
  <dep>
    <depends>cv_opticflow</depends>
  </dep>
  <header>
    <file name="orange_avoider.h"/>
  </header>
  <init fun="orange_avoider_init()"/>
  <periodic fun="orange_avoider_periodic()" freq="4"/>
  <makefile target="ap|nps">
    <file name="orange_avoider.c"/>
  </makefile>
</module>
